# Model routing configuration for AI Staff HQ
# Maps specialist roles and departments to optimal models via OpenRouter.
# Lookup order: package-local config/model_routing.yaml (preferred), then CWD/config/model_routing.yaml, otherwise defaults.

# Role-based routing (takes precedence if configured, but currently empty to respect .env default)
role_routing: {}

# Department-based fallback routing
department_routing: {}

# Budget mode (use cheaper models across the board)
# WARNING: When enabled, budget mode overrides ALL role-based and department-based routing
# All specialists will use the budget model regardless of their configured model
# Only enable this if you explicitly want to force all specialists to use a free/cheap model
budget_mode:
  enabled: false
  model: "nex-agi/deepseek-v3.1-nex-n1:free"

# Model metadata (for display and validation)
available_models:
  - id: "nex-agi/deepseek-v3.1-nex-n1:free"
    name: "DeepSeek V3 (Free)"
    context_window: 200000
    cost_per_1k_input: 0.0
    strengths: ["reasoning", "code", "analysis", "free"]

  - id: "openai/gpt-4o"
    name: "GPT-4o"
    context_window: 128000
    cost_per_1k_input: 0.005
    strengths: ["creativity", "multimodal", "code"]
